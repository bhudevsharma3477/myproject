const reducer = (state, action) => {
    switch (action.type) {
        case "SELECT_ONCHANGE_HANDLER":
            return state;
    
        default:
           return state;
    }
}

export default reducer;